{
  "name": "player",
  "version": "1.0.0",
  "description": "Player files",
  "main": "gulp",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "gulp && gulp dev",
    "clobber": "rm -rf node_modules/ && npm cache clean --force",
    "clean": "gulp clean",
    "build": "gulp"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "babel-core": "^6.26.3",
    "del": "^3.0.0",
    "gulp": "^4.0.0",
    "gulp-babel": "^8.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-uglify": "^3.0.2",
    "gulp-watch": "^5.0.0"
  }
}
